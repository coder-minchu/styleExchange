import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const SizeBottomSheet = () => {
  return (
    <View>
      <Text>SizeBottomSheet</Text>
    </View>
  )
}

export default SizeBottomSheet

const styles = StyleSheet.create({})